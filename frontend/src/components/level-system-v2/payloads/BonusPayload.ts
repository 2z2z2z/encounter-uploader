/**
 * Пейлоад для заливки бонусов
 * Будет реализовано в Шаге 25
 */

import type { PayloadBuilder } from '../types'

// Заглушка функции сборки пейлоада
export const buildBonusPayload: PayloadBuilder = (data) => {
	// Реализация в Шаге 25
	return new globalThis.URLSearchParams(data)
}


